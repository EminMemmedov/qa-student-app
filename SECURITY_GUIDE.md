# 🛡️ Руководство по защите от накрутки (Anti-Cheat System)

Комплексная защита вашего приложения от мошенничества через консоль браузера. Эти правила защитят XP, найденные баги, имена пользователей и предотвратят спам.

---

## 📋 Быстрый старт

### Шаг 1: Откройте Firebase Console
1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `qa-student-app`
3. В меню слева: **Firestore Database** → вкладка **Rules**

### Шаг 2: Скопируйте улучшенные правила
Удалите текущие правила и вставьте код ниже:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================
    // HELPER FUNCTIONS (Вспомогательные функции)
    // ============================================
    
    // Проверка: изменилось ли поле
    function fieldChanged(field) {
      return request.resource.data[field] != resource.data[field];
    }
    
    // Проверка: валидное ли имя (2-50 символов, без спецсимволов)
    function isValidName(name) {
      return name is string && 
             name.size() >= 2 && 
             name.size() <= 50 &&
             name.matches('^[a-zA-ZəƏıİöÖüÜğĞşŞçÇ\\s]+$');
    }
    
    // Проверка: валидный ли массив багов
    function isValidBugsArray(bugs) {
      return bugs is list && 
             bugs.size() <= 100; // Максимум 100 багов
    }
    
    // Проверка: не слишком ли частые обновления (rate limiting)
    function notTooFrequent() {
      return request.time > resource.data.lastUpdated + duration.value(2, 's');
    }
    
    // ============================================
    // PRODUCTION DATABASE RULES
    // ============================================
    
    match /users/{userId} {
      // Чтение доступно всем (для лидерборда)
      allow read: if true;
      
      // Создание нового пользователя
      allow create: if 
        // Имя должно быть валидным
        isValidName(request.resource.data.name) &&
        // XP должен быть 0 при создании
        request.resource.data.xp == 0 &&
        // foundBugs должен быть пустым массивом
        request.resource.data.foundBugs == [];
      
      // Обновление существующего пользователя
      allow update: if 
        // ===== ЗАЩИТА XP =====
        // 1. XP не может уменьшаться
        request.resource.data.xp >= resource.data.xp &&
        
        // 2. Нельзя добавить больше 500 XP за раз
        (request.resource.data.xp - resource.data.xp) <= 500 &&
        
        // ===== ЗАЩИТА FOUND BUGS =====
        // 3. foundBugs должен быть валидным массивом
        isValidBugsArray(request.resource.data.foundBugs) &&
        
        // 4. Нельзя удалять найденные баги (только добавлять)
        request.resource.data.foundBugs.size() >= resource.data.foundBugs.size() &&
        
        // 5. Нельзя добавить больше 10 багов за раз
        (request.resource.data.foundBugs.size() - resource.data.foundBugs.size()) <= 10 &&
        
        // ===== ЗАЩИТА ИМЕНИ =====
        // 6. Если имя меняется, оно должно быть валидным
        (!fieldChanged('name') || isValidName(request.resource.data.name)) &&
        
        // ===== RATE LIMITING =====
        // 7. Нельзя обновлять профиль чаще, чем раз в 2 секунды
        (!fieldChanged('xp') && !fieldChanged('foundBugs') || notTooFrequent());
      
      // Удаление запрещено
      allow delete: if false;
    }

    // ============================================
    // TEST DATABASE (для разработки)
    // ============================================
    match /users_test/{userId} {
      allow read, write: if true;
    }
  }
}
```

### Шаг 3: Опубликуйте правила
Нажмите **"Publish"** — правила вступят в силу мгновенно! ⚡

---

## 🔒 Что защищают эти правила?

### 1. **Защита XP от накрутки**
- ❌ Нельзя уменьшить XP
- ❌ Нельзя добавить больше 500 XP за раз
- ✅ Можно только постепенно увеличивать

**Пример атаки:**
```javascript
// Попытка накрутки
updateDoc(userRef, { xp: 999999 });
// ❌ ОТКЛОНЕНО: превышен лимит +500 XP
```

### 2. **Защита найденных багов**
- ❌ Нельзя удалять найденные баги
- ❌ Нельзя добавить больше 10 багов за раз
- ❌ Максимум 100 багов всего
- ✅ Можно только добавлять постепенно

**Пример атаки:**
```javascript
// Попытка накрутки багов
updateDoc(userRef, { foundBugs: Array(50).fill('fake_bug') });
// ❌ ОТКЛОНЕНО: превышен лимит +10 багов
```

### 3. **Защита имени пользователя**
- ❌ Нельзя использовать спецсимволы
- ❌ Нельзя использовать слишком короткие имена (<2 символов)
- ❌ Нельзя использовать слишком длинные имена (>50 символов)
- ✅ Только буквы (латиница + азербайджанские символы) и пробелы

**Пример атаки:**
```javascript
// Попытка спама
updateDoc(userRef, { name: '💩💩💩 HACK 💩💩💩' });
// ❌ ОТКЛОНЕНО: недопустимые символы
```

### 4. **Rate Limiting (ограничение частоты)**
- ❌ Нельзя обновлять XP/баги чаще, чем раз в 2 секунды
- ✅ Защита от автоматических скриптов

**Пример атаки:**
```javascript
// Попытка спама обновлениями
for (let i = 0; i < 100; i++) {
  updateDoc(userRef, { xp: increment(10) });
}
// ❌ ОТКЛОНЕНО: слишком частые обновления
```

---

## 🚨 Что увидит нарушитель?

При попытке накрутки в консоли браузера появится ошибка:

```
FirebaseError: Missing or insufficient permissions.
Code: permission-denied
```

или

```
FirebaseError: PERMISSION_DENIED: 
False for 'update' @ L30
```

---

## 📊 Мониторинг подозрительной активности

### В Firebase Console:
1. Перейдите в **Firestore Database** → **Usage**
2. Следите за аномальными всплесками записей
3. Проверяйте логи в **Cloud Logging** (если включен Blaze план)

### Подозрительные паттерны:
- 🚩 Пользователь с XP > 50,000 за день
- 🚩 Более 20 найденных багов за час
- 🚩 Частые изменения имени (>5 раз в день)
- 🚩 Одинаковые имена у разных пользователей

---

## 🎯 Продвинутая защита (Level 2)

Для максимальной безопасности используйте **Cloud Functions** (требуется тариф **Blaze**):

### Преимущества:
- ✅ Вся логика на сервере
- ✅ Клиент вообще не может писать XP напрямую
- ✅ Детальное логирование всех действий
- ✅ Возможность банить читеров

### Пример Cloud Function:
```javascript
exports.addXP = functions.https.onCall(async (data, context) => {
  const { userId, xpToAdd, reason } = data;
  
  // Валидация
  if (xpToAdd > 100) throw new Error('Too much XP');
  
  // Обновление с логированием
  await db.collection('users').doc(userId).update({
    xp: admin.firestore.FieldValue.increment(xpToAdd),
    lastUpdated: admin.firestore.FieldValue.serverTimestamp()
  });
  
  // Лог для аудита
  await db.collection('xp_logs').add({
    userId, xpToAdd, reason, timestamp: new Date()
  });
});
```

---

## ✅ Чеклист безопасности

- [x] Firebase Security Rules настроены
- [x] Защита XP от накрутки
- [x] Защита foundBugs от манипуляций
- [x] Валидация имен пользователей
- [x] Rate limiting включен
- [ ] Cloud Functions для критичных операций (опционально)
- [ ] Мониторинг аномалий настроен (опционально)
- [ ] Регулярный аудит топ-пользователей (рекомендуется)

---

## 🆘 Troubleshooting

### Проблема: Легитимные пользователи не могут обновить профиль
**Решение:** Проверьте, что `lastUpdated` обновляется при каждом изменении в `useGameProgress.js`

### Проблема: Правила слишком строгие
**Решение:** Временно увеличьте лимиты (например, 1000 XP вместо 500) и мониторьте

### Проблема: Тестирование невозможно
**Решение:** Используйте коллекцию `users_test` для разработки

---

## 📚 Дополнительные ресурсы

- [Firebase Security Rules Documentation](https://firebase.google.com/docs/firestore/security/get-started)
- [Security Rules Testing](https://firebase.google.com/docs/rules/unit-tests)
- [Best Practices](https://firebase.google.com/docs/firestore/security/rules-conditions)

---

**Последнее обновление:** 2025-12-02  
**Версия правил:** 2.0 (Enhanced Security)
